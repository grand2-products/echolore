pre-commit:
  parallel: true
  jobs:
    - name: biome check
      run: git diff --cached --name-only --diff-filter=d -- '*.ts' '*.tsx' '*.json' '*.css' | xargs -r pnpm biome check --no-errors-on-unmatched

    - name: typecheck
      run: pnpm typecheck
